import { BrowserRouter, Switch, Route } from "react-router-dom";

const Router: React.FC = () => {
  return (
    <BrowserRouter>
      <Switch></Switch>
    </BrowserRouter>
  );
};

export default Router;
